# Node with prisma ORM tool
- git clone
- npm install

- npm i -d prisma --> dev depenedency
- npx prisma init
    it generate .env file and prisma folder. In prisma folder we have one file called schema.prisma
- In schema.prisma
    we added database type, database url and make new schema i.e modal
    In model -> ? for optional field
- npm install @prisma/client
